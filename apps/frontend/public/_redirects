# Render URL Rewrites
# https://docs.render.com/redirects-rewrites

# 1. API Proxy (Must be first!)
# Rewrites /api/* to the backend API
/api/*  https://financial-planner-api.onrender.com/api/:splat  200

# 2. SPA Catch-all (Must be last!)
# Rewrites everything else to index.html
/*  /index.html  200
